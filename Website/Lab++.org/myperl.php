<?php
echo exec('whoami');
	$cmd = "/usr/bin/perl -e /customers/4/6/f/labpp.org/httpd.www/perlscript.pl";
	exec($cmd, $output, $result);
		print $cmd;
		print $output;
		print $result;
		
	$cmd = "ls";
	exec($cmd, $output, $result);
		print $cmd;
		print $output;
		print $result;
?>